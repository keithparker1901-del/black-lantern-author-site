document.addEventListener('DOMContentLoaded',()=>{const cover=document.querySelector('.cover-button');if(cover){cover.addEventListener('click',()=>{const img=cover.querySelector('img');const viewer=document.createElement('div');viewer.className='cover-viewer';viewer.setAttribute('role','dialog');viewer.setAttribute('aria-modal','true');viewer.setAttribute('aria-label','The Salt Road That Fed the Dragon cover');viewer.innerHTML=`<button class="cover-viewer-close" type="button" aria-label="Close cover viewer">×</button><img src="${img.src}" alt="${img.alt}"><p>The Salt Road That Fed the Dragon</p>`;document.body.append(viewer);document.body.classList.add('viewer-open');const close=()=>{viewer.remove();document.body.classList.remove('viewer-open');document.removeEventListener('keydown',keys)};const keys=e=>{if(e.key==='Escape')close()};viewer.addEventListener('click',e=>{if(e.target===viewer||e.target.closest('.cover-viewer-close'))close()});document.addEventListener('keydown',keys);viewer.querySelector('.cover-viewer-close').focus();});}const hero=document.querySelector('.salt-hero');if(hero&&!matchMedia('(prefers-reduced-motion: reduce)').matches){addEventListener('scroll',()=>{hero.style.setProperty('--salt-scroll',Math.min(scrollY*.08,40)+'px')},{passive:true});}});