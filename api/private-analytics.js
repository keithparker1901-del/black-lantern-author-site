const { loadEvents } = require('../lib/analytics-store');
function destination(url){try{const u=new URL(url);if(/amazon\./i.test(u.hostname))return'Amazon';if(/goodreads/i.test(u.hostname))return'Goodreads';if(/bookbub/i.test(u.hostname))return'BookBub';if(/facebook|fb\.com/i.test(u.hostname))return'Facebook';return u.hostname.replace(/^www\./,'')}catch{return'Other'}}
module.exports=async function handler(req,res){res.setHeader('Cache-Control','private, no-store, max-age=0');res.setHeader('X-Robots-Tag','noindex, nofollow, noarchive');if(req.method!=='GET'){res.setHeader('Allow','GET');return res.status(405).json({ok:false})}try{const {configured,events}=await loadEvents(5000);if(!configured)return res.status(503).json({ok:false,configured:false,message:'Analytics storage is not configured.'});const pageviews=events.filter(e=>e.event==='pageview'),clicks=events.filter(e=>e.event==='outbound_click'||e.marker==='LANTERN_OUTBOUND_ATTRIBUTION'),visitors=new Set(pageviews.map(e=>e.visitorId).filter(Boolean)),clickers=new Set(clicks.map(e=>e.visitorId).filter(Boolean)),days={};for(const e of pageviews){const d=String(e.at||'').slice(0,10);if(d)days[d]=(days[d]||0)+1}const byDestination={};for(const e of clicks){const d=destination(e.url);byDestination[d]=(byDestination[d]||0)+1}const recent=clicks.sort((a,b)=>String(b.at).localeCompare(String(a.at))).slice(0,25).map(e=>({destination:destination(e.url),label:e.label||'Outbound link',attribution:e.attribution||'Direct',pagePath:e.pagePath||'/',rawPagePath:e.rawPagePath||e.pagePath||'/',campaign:e.campaign||'',at:e.at}));return res.status(200).json({ok:true,configured:true,totalVisits:pageviews.length,uniqueVisitors:visitors.size,totalOutboundClicks:clicks.length,uniqueClickers:clickers.size,days,byDestination,recent})}catch(error){console.error(error);return res.status(500).json({ok:false,message:'Analytics report could not be loaded.'})}}
