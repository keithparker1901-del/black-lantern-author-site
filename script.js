(()=>{const y=document.querySelectorAll('[data-year]');y.forEach(n=>n.textContent=new Date().getFullYear());const t=document.querySelector('.nav-toggle');const n=document.querySelector('.site-nav');if(t&&n){t.addEventListener('click',()=>{const open=n.classList.toggle('open');t.setAttribute('aria-expanded',String(open))});n.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{n.classList.remove('open');t.setAttribute('aria-expanded','false')}))}const search=document.querySelector('#catalog-search');const filters=[...document.querySelectorAll('.filter')];const items=[...document.querySelectorAll('.catalog-item')];const empty=document.querySelector('#catalog-empty');let active='all';function apply(){if(!items.length)return;const q=(search?.value||'').trim().toLowerCase();let shown=0;items.forEach(item=>{const matchSeries=active==='all'||item.dataset.series===active;const matchText=!q||(item.dataset.search||item.textContent).toLowerCase().includes(q);const show=matchSeries&&matchText;item.hidden=!show;if(show)shown++});if(empty)empty.hidden=shown!==0}filters.forEach(b=>b.addEventListener('click',()=>{filters.forEach(x=>x.classList.remove('active'));b.classList.add('active');active=b.dataset.filter||'all';apply()}));search?.addEventListener('input',apply);})();